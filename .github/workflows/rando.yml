name: Yahtzee

on:
  push:
    branches: [main]


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - id: Roll_first_dice
      run: echo "FIRSTNUM=$(ruby -e 'puts rand(1..6)')" >> $GITHUB_ENV
      shell: bash
    - uses: actions/checkout@v2
    - id: Print_first_num
      run: echo $FIRSTNUM
      shell: bash
